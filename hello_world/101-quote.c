#include <stdio.h>

/**
 * main - Print quote
 * Description: Prints a quote with restrictions to functions
 * Return: 1 (Success)
 */
int main(void)
{
	fwrite("and that piece of art is useful\n", 1, 32, stderr);

	return (1);
}
