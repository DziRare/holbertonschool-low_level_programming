#include <unistd.h>

/**
 * main - Print quote
 * Description: Prints a quote with restrictions to functions
 * Return: 1 (Success)
 */
int main(void)
{
	write(1, "and that piece of art is useful\n", 32);
	return (1);
}
